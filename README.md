# ceph-storage-provession-check
~~~
This commmand will show ceph rbd all block device all volumes and thin provessioned with used capacity.
rbd du -p volumes-ssd >> volumes-ssd-gk
rbd du -p volumes >> volumes-gk
~~~
